import savedJobController, {
  saveJob,
  unsaveJob,
  getUserSavedJobs,
  checkIsJobSaved,
} from '../savedJob/savedJob.controller.js';

export {
  saveJob,
  unsaveJob,
  getUserSavedJobs,
  checkIsJobSaved,
};

export default savedJobController;
